import { atom } from "jotai";

// const countAtom = atom(0);
//
// const countryAtom = atom('Japan');
//
// const citiesAtom = atom(['Tokyo', 'Kyoto', 'Osaka']);

const count = 0;

export const countAtom = atom(count);

export const initialAtom = atom(0);
